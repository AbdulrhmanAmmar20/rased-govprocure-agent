# Pinned floor for the sovereign runtime image.
# Mirror these wheels into the in-Kingdom artefact registry; the build must
# never resolve packages from a public index at deploy time (NFR-1.1).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
PyJWT>=2.8
cryptography>=42.0
httpx>=0.27
numpy>=1.26
